const nailThunder = (game, players, phase) => {
  players.forEach(player => {
    const thunderImage = this.add.image(canvasWidth / 2 + 40, canvasHeight / 2 + 100, 'dpsChar')
  })
}

export default nailThunder