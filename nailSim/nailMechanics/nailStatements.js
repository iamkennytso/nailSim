const nailStatements = (phase) => {
  switch (phase) {
    case 1:
      const isStack = !!Math.round(Math.random)
      setTimeout(() => {
        nailIn()
      }, 6000)
  }
}

export default nailStatements